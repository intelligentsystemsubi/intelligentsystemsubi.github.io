source "https://rubygems.org"

# Use GitHub Pages gem to ensure compatibility with GitHub Pages CI
gem "github-pages", group: :jekyll_plugins

# Optional (only needed if you're using Ruby features outside GitHub Pages defaults)
gem "bigdecimal", "~> 3.1"
gem "logger", "~> 1.7"

# Optional for Windows platforms
# gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
